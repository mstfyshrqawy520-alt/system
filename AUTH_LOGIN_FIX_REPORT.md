# تقرير إصلاح تسجيل الدخول

## المشكلة
ظهرت رسالة «بيانات الدخول غير صحيحة» عند محاولة الدخول بحساب `amar@gmail.com`.

## التشخيص
كانت اختبارات Laravel قد أعادت تهيئة قاعدة البيانات المحلية، لذلك لم تكن حسابات Demo الرسمية مضمونة في الحالة الحالية. مسار تسجيل الدخول الصحيح هو:

`POST /api/v1/auth/login`

وليس `/api/v1/login`.

## الإصلاح
تم تشغيل:

- `DemoUserSeeder`
- `SeedTwoHundredPurchaseOperationsSeeder`

فعادت الحسابات الرسمية وكلمة المرور الموحدة `123456`، مع إعادة بيانات العمليات التجريبية البالغ عددها 200 عملية.

## التحقق

تم اختبار API مباشرة:

- البريد: `amar@gmail.com`
- كلمة المرور: `123456`
- النتيجة: HTTP 200
- الدور: `employee`

ثم تم اختبار الدخول من Chrome بنجاح، وظهرت لوحة الموظف باسم **عمار** على المسار `/employee`، مع ظهور 15 طلبًا معتمدًا وواجهة النظام بشكل طبيعي.

أثناء فحص جميع الحسابات ظهر أن حساب `hasan@gmail.com` لم يكن موجودًا في `DemoUserSeeder`، فتمت إضافته بدور `accountant`، ثم نجح تسجيل دخوله عبر API بحالة HTTP 200. بعد الإصلاح نجحت جميع الحسابات الرئيسية السبعة بحالة HTTP 200.

## الحسابات الرئيسية المتاحة

| الحساب | الدور | كلمة المرور |
|---|---|---|
| `amar@gmail.com` | موظف | `123456` |
| `safa@gmail.com` | موظف | `123456` |
| `zaid@gmail.com` | موظف | `123456` |
| `ahmed@gmail.com` | مدير المشتريات | `123456` |
| `hasan@gmail.com` | الحسابات | `123456` |
| `mohamed@gmail.com` | المدير العام | `123456` |
| `admin@gmail.com` | مدير النظام | `123456` |
