# تقرير تنفيذ مركز مراقبة Admin

## الحالة

تم تنفيذ مركز مراقبة وتشغيل داخل نظام مشتريات الإشبيليّة لمتابعة حالة التطبيق أثناء الـDeploy وبعد الانتقال إلى Production، دون تعديل دورة المشتريات أو حالات الاعتماد أو صلاحيات الموظف والمراجع ومدير المشتريات والحسابات والمدير العام.

## ما تم إضافته

| الجزء | التنفيذ |
|---|---|
| صفحة React | إضافة `/admin/system-monitor` داخل مسارات Admin المحمية بدور `admin`. |
| رابط التنقل | إضافة «مراقبة النظام والـDeploy» في Sidebar الخاص بالـAdmin. |
| لوحة Admin الرئيسية | إضافة بطاقة وصول مباشرة إلى مركز المراقبة. |
| Health Check | فحص حالة التطبيق، اتصال قاعدة البيانات، زمن Query، حالة Migrations، وسلامة حقول رقم القطعة والمنطقة. |
| Database Monitor | عرض المحرك، الاتصال، زمن الاستجابة، وعدد السجلات الرئيسية. |
| Migration Monitor | عرض عدد الـMigrations المطبقة والمعلقة وأسماء المعلقة عند وجودها. |
| Realtime Monitor | عرض حالة مسار SSE وآخر System Event، مع عدم اختلاق عدد الاتصالات عندما لا توجد Telemetry حقيقية. |
| Deployment Monitor | عرض الإصدار وCommit ووقت الـDeploy إذا تم تعريف متغيرات البيئة. |
| Alerts | تنبيهات حرجة عند انقطاع DB أو وجود Migrations معلقة أو بيانات تتبع ناقصة، وتنبيهات للـFailed Jobs وبيانات Deploy غير المربوطة. |
| Workflow Monitor | عرض توزيع حالات طلبات الشراء وأوامر الشراء. |
| Audit Log | عرض آخر 100 System Event مع الفاعل والكيان. |
| صلاحيات | إضافة `system.monitor.view` ومنحها لدور Admin عبر `RolePermissionSeeder`. |
| Polling | تحديث البيانات تلقائيًا كل 30 ثانية مع زر «إعادة الفحص الآن». |
| التجاوب | الصفحة مبنية بتخطيط Responsive للموبايل والتابلت واللابتوب. |

## المسارات الخلفية

```text
GET /api/v1/admin/system/monitoring
GET /api/v1/admin/system/health
GET /api/v1/admin/system/alerts
GET /api/v1/admin/system/audit-log
```

كل المسارات محمية بواسطة `auth:sanctum` و`system.monitor.view`. المستخدم غير المصادق عليه يحصل على 401، والمستخدم غير المصرح له يحصل على 403.

## متغيرات الإصدار الاختيارية

تمت إضافة `config/deployment.php` لقراءة:

```env
APP_VERSION=2026.08.17.1
APP_COMMIT=رقم-Commit
APP_DEPLOYED_AT=2026-08-17T12:00:00+03:00
```

إذا لم يتم تعريفها، تظهر الصفحة «غير محدد» مع تنبيه معلوماتي فقط، ولا تعتبر ذلك عطلًا في النظام.

## حماية البيانات

لا تعرض صفحة المراقبة كلمات المرور أو قيم `.env` أو Stack Trace كامل. كما أن بيانات API الخاصة بالمستخدمين لا يتم تخزينها في Cloudflare Cache. مسار SSE واستجابات API المصادق عليها يجب أن تمر مباشرة إلى Laravel دون تخزين مؤقت.

## نتائج الاختبارات

| الفحص | النتيجة |
|---|---|
| Laravel/PHPUnit | **142 اختبارًا ناجحًا، 415 assertion** |
| اختبارات المراقبة الخلفية | **4 اختبارات ناجحة** |
| TypeScript | ناجح بدون أخطاء |
| Vitest | **10 ملفات اختبار ناجحة، 66 اختبارًا ناجحًا** |
| Production Build | ناجح، تم تحويل **194 وحدة** |
| Routes | تم تسجيل 4 مسارات جديدة للمراقبة |
| صلاحيات | تم تشغيل `RolePermissionSeeder` وإضافة الصلاحية الجديدة لقاعدة Pilot |

## ملاحظات التشغيل

عدد اتصالات SSE اللحظية وP95 latency التفصيلي لا يظهران كأرقام تقديرية. يعرض النظام ملاحظة بأن Telemetry إضافية مطلوبة إذا أردنا قياسهما بدقة. هذا يمنع ظهور بيانات مضللة داخل لوحة Admin.

لم يتم تنفيذ Rollback أو حذف بيانات أو تفعيل وضع صيانة من داخل الصفحة؛ هذه الإجراءات تظل خارج الواجهة في المرحلة الأولى لتقليل المخاطر التشغيلية.
