# متطلبات Queue قبل النشر

تم إعداد إشعارات النظام لتُرسل عبر `SendNotificationsJob` بعد اكتمال المعاملة، وتم ضبط `QUEUE_CONNECTION=redis` في قالب بيئة الإنتاج.

قبل نشر النظام، يجب التأكد من توفر Redis أو خدمة Redis متوافقة، ثم تشغيل عامل Laravel مستمر لمعالجة الوظائف:

```bash
php artisan queue:work redis --sleep=3 --tries=3 --timeout=90
```

يجب تشغيل العامل كعملية مستمرة وإعادة تشغيله تلقائيًا عند سقوطه أو بعد نشر نسخة جديدة. على VPS يمكن استخدام Supervisor أو systemd، أما على الاستضافة المشتركة فيجب استخدام آلية Background Process أو Cron المتاحة في خطة الاستضافة. لا يكفي وضع `QUEUE_CONNECTION=redis` بدون عامل يعمل؛ ستتراكم الإشعارات في الطابور ولن تظهر للمستخدمين حتى تتم معالجتها.

بعد كل نشر، استخدم:

```bash
php artisan queue:restart
```

ثم تحقق من الوظائف الفاشلة:

```bash
php artisan queue:failed
```

إذا كانت الاستضافة لا توفر Redis أو عملية worker مستمرة، استخدم `QUEUE_CONNECTION=database` بعد تجهيز جدول `jobs` وتشغيل worker بالطريقة التي تسمح بها الاستضافة، أو أبقِ `sync` مؤقتًا في بيئة الاختبار فقط. لا تستخدم `sync` كخيار إنتاجي إذا كان إرسال الإشعارات يؤثر على زمن استجابة الطلبات.
