# تقرير فرض إلزامية رقم القطعة والمنطقة

**المشروع:** نظام مشتريات شركة الإشبيليّة

**تاريخ التنفيذ:** 17 أغسطس 2026

## الملخص التنفيذي

تم تطبيق تشديد كامل على حقلي **رقم القطعة (`item_reference`)** و**المنطقة (`region`)** في بنود طلبات الشراء وبنود أوامر الشراء. أصبح القيد مفروضًا على ثلاث طبقات متكاملة: واجهة الـAPI، وطبقة الخدمات، وقاعدة البيانات. لم يتم تغيير دورة المشتريات أو الصلاحيات أو الحالات التشغيلية؛ اقتصر التعديل على حماية البيانات وإصلاح بيانات الاختبارات.

> أي طلب أو أمر شراء جديد لا يمكن حفظ أحد بنوده بدون رقم قطعة ومنطقة صالحين، حتى إذا تم تجاوز واجهة React واستدعاء الـAPI مباشرة.

## التعديلات المنفذة

| المجال | الملف أو المكوّن | التعديل |
|---|---|---|
| قاعدة البيانات | `database/migrations/2026_08_17_000002_enforce_reference_fields_not_null.php` | تحويل القيم التاريخية `NULL` أو الفارغة إلى `UNKNOWN` ثم جعل الحقلين `NOT NULL` في جدولي `purchase_request_items` و`purchase_order_items`. |
| إنشاء طلب الشراء | `app/Services/PurchaseRequestService.php` | تشديد التحقق المركزي داخل `calculateFinancials()` لمنع حفظ رقم قطعة أو منطقة فارغة. |
| أوامر الشراء | `app/Services/PurchaseOrderService.php` | إضافة `requireReferenceFields()` وتطبيقها على إنشاء أمر من طلب، وتعديل البند، وإضافة بند. |
| المراجع | `app/Services/ReviewerPurchaseRequestService.php` | إضافة تحقق مركزي عند تعديل بند أو إضافة بند، مع رسائل Validation عربية واضحة. |
| اختبارات الإشعارات | `tests/Feature/Api/V1/NotificationTest.php` | إضافة رقم القطعة والمنطقة إلى البنود اليدوية في اختبارات دورة الإشعارات. |
| اختبارات المدير العام | `tests/Feature/Api/V1/GeneralManagerPurchaseOrderTest.php` | استكمال بيانات بند أمر الشراء في بيانات الاختبار المشتركة. |
| اختبارات العلاقات | `tests/Unit/Models/ModelRelationshipsTest.php` | استكمال بيانات بنود PR وPO اليدوية بالحقول الجديدة. |

## سلوك البيانات التاريخية

قبل فرض القيد، تقوم الـMigration بمعالجة أي قيمة `NULL` أو سلسلة فارغة أو مسافات فقط باستخدام القيمة المؤقتة `UNKNOWN`. هذه القيمة مخصصة للسجلات التاريخية التي لم يكن النظام القديم يسجل لها بيانات التتبع، ولا تُستخدم كبديل عن التحقق في الطلبات الجديدة.

## نتائج بيانات Pilot بعد إعادة التجهيز

| البيان | العدد |
|---|---:|
| المستخدمون | 11 |
| طلبات الشراء | 200 |
| أوامر الشراء | 200 |
| بنود طلبات الشراء | 399 |
| بنود أوامر الشراء | 399 |
| أحداث النظام | 1,186 |
| بنود PR ذات `item_reference = NULL` | 0 |
| بنود PR ذات `region = NULL` | 0 |
| بنود PO ذات `item_reference = NULL` | 0 |
| بنود PO ذات `region = NULL` | 0 |
| بنود PR ذات قيمة فارغة | 0 |
| بنود PO ذات قيمة فارغة | 0 |

تم تشغيل `BackfillSystemEventsSeeder` بصورة آمنة؛ وبما أن سجل الأحداث موجود بالفعل، تم تخطي إعادة الترحيل للحفاظ على عدم التكرار. هذا السلوك يجعل Seeder قابلًا لإعادة التشغيل دون مضاعفة الأحداث التاريخية.

## نتائج الاختبارات النهائية

| الفحص | النتيجة |
|---|---|
| Laravel/PHPUnit | **138 اختبارًا ناجحًا، 374 assertion** خلال 10.21 ثانية. |
| TypeScript | ناجح بدون أخطاء. |
| Production Build | ناجح، وتم تحويل 192 وحدة. |
| Vitest | **9 ملفات اختبار ناجحة**. |
| Migration | `Nothing to migrate` بعد التطبيق؛ القيد مطبق بالفعل. |
| SQLite schema check | `item_reference` و`region` يظهران بقيمة `notnull = 1` في جدولي PR وPO items. |

## التوافق مع PostgreSQL

تعتمد الـMigration على Query Builder وSchema Builder في Laravel، ولا تستخدم صيغة SQL خاصة بـSQLite. تحديث القيم القديمة يتم عبر `whereNull` و`TRIM`، ثم يتم تغيير تعريف الحقول بواسطة `change()`، ولذلك يمكن تشغيلها ضمن مسار PostgreSQL المحدد للإنتاج. يجب تشغيل Migration مرة واحدة في بيئة الإنتاج باستخدام:

```bash
php artisan migrate --force
```

لا يجب استخدام `migrate:fresh` على قاعدة الإنتاج لأنه يحذف بيانات الشركة.

## أثر التعديل على دورة المشتريات

لم يحدث أي تغيير في التدفق المتفق عليه:

1. الموظف ينشئ طلب شراء ويرسل رقم القطعة والمنطقة داخل كل بند.
2. المراجع يرى البيانات ويعدلها أو يعتمد أو يرفض حسب الصلاحيات الحالية.
3. مدير المشتريات يحول الطلب المعتمد إلى أمر شراء ويستكمل البيانات المالية.
4. يتم إصدار الأمر للحسابات وفق السلوك الحالي.
5. تصل الإشعارات للحسابات والمدير العام وفق دورة النظام الحالية.

التعديل الوحيد هو أن الحقول المطلوبة لا يمكن تجاوزها في أي مسار كتابة.

## توصية التشغيل قبل Production

يُوصى بتشغيل Migration على قاعدة PostgreSQL التجريبية أولًا، ثم التحقق من عدد الصفوف والقيم الفارغة، ثم تشغيل اختبارات Laravel قبل فتح النظام للمستخدمين. بعد ذلك يجب أخذ نسخة احتياطية من PostgreSQL قبل أي Seeder أو Backfill إضافي، مع إبقاء Cache Cloudflare معطلًا عن الاستجابات الخاصة والمصادق عليها كما هو موثق في دليل النشر.
