import React from 'react'; import {describe,it,expect,vi,beforeEach}from'vitest';import{render,screen,waitFor}from'@testing-library/react';import{MemoryRouter}from'react-router-dom';import ProcurementDashboardPage from'../pages/procurement/ProcurementDashboardPage';import*as procurement from'../api/procurement';import*as orders from'../api/purchaseOrders';
describe('Procurement purchase order frontend',()=>{beforeEach(()=>vi.restoreAllMocks());it('renders the procurement dashboard from API data',async()=>{vi.spyOn(procurement,'getApprovedPurchaseRequestsApi').mockResolvedValue([]);vi.spyOn(orders,'getPurchaseOrdersApi').mockResolvedValue({ data: [], meta: { current_page: 1, from: null, last_page: 1, per_page: 15, to: null, total: 0 } });render(<MemoryRouter><ProcurementDashboardPage/></MemoryRouter>);await waitFor(()=>expect(screen.getByText(/إدارة المشتريات/i)).toBeInTheDocument())})});
