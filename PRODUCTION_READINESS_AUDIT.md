# تدقيق الجاهزية الإنتاجية لنظام مشتريات الإشبيليّة

**الإصدار:** 1.0 — **التاريخ:** 15 أغسطس 2026 — **إعداد:** Manus AI  
**نطاق التدقيق:** واجهة React، وواجهة Laravel API، وقاعدة البيانات، والمصادقة والصلاحيات، والإشعارات اللحظية، وتجربة الاستخدام، والاستجابة للشاشات، والطباعة، والاختبارات، وتجهيزات النشر خلف Cloudflare Free.

## 1. الخلاصة التنفيذية

النظام يعمل تقنيًا في بيئة التطوير، ودورة المشتريات الأساسية منفذة ومغطاة باختبارات ناجحة. كما أن المتطلبات التجارية الحساسة محفوظة: الموظف ينشئ طلبًا، والمراجع المختار من الموظف يراجعه ضمن نطاق قسمه، ثم يمر الطلب إلى مدير المشتريات، ويصدر أمر الشراء وتصل الإشعارات إلى الحسابات والمدير العام للعرض فقط. لا يوصي هذا التدقيق بتغيير هذه الدورة أو منطق الصلاحيات أو انتقالات الحالات.

في المقابل، لا ينبغي اعتبار النظام جاهزًا للإطلاق الإنتاجي النهائي قبل إغلاق فجوات **P0 وP1**. أهمها أن بعض رسائل الأخطاء ما زالت تقنية أو إنجليزية، ولا توجد حالات خطأ عربية مخصصة لمسارات 404 و403 و500، وصفحة التفضيلات لا تفصل حاليًا بين الإشعارات الإلزامية والاختيارية. توجد كذلك تحسينات مهمة في حالات التحميل والفراغ والبحث والفلترة ومنع طلبات API المكررة، لكنها لا تمنع تشغيل الدورة الأساسية ويمكن تنفيذها على مراحل.

> **الحكم الحالي:** جاهز للتجربة الداخلية والاختبار التشغيلي، وغير جاهز بعد للإطلاق الإنتاجي العام قبل تنفيذ P0، ثم التحقق من P1 على الهاتف والكمبيوتر، ثم استكمال قائمة النشر والنسخ الاحتياطي والمراقبة.

## 2. تعريف النطاق وحدود التغيير

تم إجراء التدقيق على أساس أن تحسين الجاهزية يعني رفع سهولة الاستخدام والموثوقية والأمان وقابلية النشر، وليس إعادة تصميم دورة الشراء. العناصر التالية خارج نطاق التعديل الوظيفي في هذه المرحلة: دورة Purchase Request، وسير الموافقات، وعزل المراجع حسب القسم، وأدوار المستخدمين وصلاحياتهم، وانتقالات الحالات، والحسابات المالية، وقيم الوحدات الداخلية، وغياب Tax/VAT/Discount.

| قاعدة لا يجوز المساس بها | قرار التدقيق |
|---|---|
| الموظف يختار المراجع عند إنشاء الطلب | محفوظة |
| المراجع يرى الطلبات المسندة إليه فقط | محفوظة ويجب اختبارها قبل كل إصدار |
| مدير المشتريات صاحب القرار النهائي | محفوظ |
| الحسابات والمدير العام View Only مع إشعار | محفوظ |
| أمر الشراء بالجنيه المصري EGP دون ضرائب أو خصومات | محفوظ |
| رقم القطعة والمنطقة حقول إلزامية | محفوظ |
| الواجهة عربية وRTL والوحدات معروضة بالعربية | محفوظ |
| الإشعارات اللحظية عبر SSE دون Refresh | محفوظة مع الحاجة إلى اختبار Proxy/Timeout |

## 3. البنية الحالية والميزات المنفذة

### 3.1 المكدس التقني

| الطبقة | الحالة الحالية |
|---|---|
| Frontend | React 18 + TypeScript + Vite + TailwindCSS |
| Backend | Laravel 11 + PHP 8.2 + Sanctum |
| Database | SQLite محليًا؛ يوصى بـ MySQL أو PostgreSQL في الإنتاج |
| Authorization | صلاحيات مخصصة عبر Middleware، دون Spatie |
| Notifications | API للإشعارات وSSE في `/api/v1/notifications/stream` |
| Printing | نافذة طباعة مستقلة مع عزل مستند الطباعة وخط Cairo عربي عريض |
| Deployment | ملفات Nginx وSupervisor وCloudflare Free موثقة داخل `deploy/` |
| Testing | Laravel Feature Tests وVitest للواجهة |

### 3.2 الوحدات ومسارات API الأساسية

توجد وحدات المصادقة، طلبات الشراء، مراجعة الطلبات، إدارة أوامر الشراء، الحسابات، المدير العام، الإشعارات، الموردين، الكتالوج، التحليلات، وإدارة المستخدمين والأقسام. وتشمل المسارات المهمة إنشاء الطلب وإرساله، عزل قائمة المراجع، المراجعة والتعديل والاعتماد والرفض، اعتماد مدير المشتريات، إنشاء وتعديل أمر الشراء، عرض الحسابات والمدير العام، وبث الإشعارات اللحظية.

المسارات الحساسة محمية بواسطة `auth:sanctum` وصلاحيات مثل `purchase_request.view_assigned` و`purchase_request.approve` و`purchase_order.create` و`purchase_order.view_accounting` و`purchase_order.view_gm`. هذه الحماية يجب أن تظل مصدر القرار النهائي، ولا يجوز الاعتماد على إخفاء الأزرار في الواجهة فقط.

### 3.3 الميزات التي تم تنفيذها وتحققها

| المجال | ما تم إنجازه |
|---|---|
| البيانات الأساسية | إزالة اسم المشروع وعنوان الطلب وفق القرار التجاري، والإبقاء على البيانات المطلوبة للدورة الحالية |
| البنود | رقم القطعة والمنطقة إلزاميان وموجودان في النماذج والتفاصيل والطباعة |
| اختيار المراجع | الموظف يختار رئيس القسم/المراجع المسجل في النظام |
| عزل المراجع | المراجع لا يرى طلبات خارج الطلبات المسندة إليه حسب الصلاحية والمنطق الخلفي |
| أمر الشراء | بيانات المورد واسم الصنف والكمية والوحدة والسعر والإجمالي محفوظة للطباعة والعرض |
| الحسابات والمدير العام | وصول للعرض والإشعار وفق القاعدة التجارية المعتمدة |
| الطباعة | مستند منفصل، صفحة A4 واحدة، شعار، خط عربي عريض، وحدات عربية، خانات توقيع، إجماليات رأسية، وصفوف إضافية |
| الإشعارات | إشعارات داخلية وبث SSE دون الحاجة إلى Refresh، مع ضرورة اختبار الانقطاع وإعادة الاتصال |
| التفضيلات | وضع عرض مضغوط، تقليل الحركة، وتأكيد الإجراءات الحساسة محفوظة محليًا |
| المستخدمون التجريبيون | حسابات رسمية موحدة بكلمة المرور `123456` للتجربة، ويجب استبدالها أو فرض تغييرها في الإنتاج |
| البيانات التجريبية | Seeder لحسابات Demo وSeeder لـ 200 عملية شراء و20 موردًا و300 عنصر كتالوج |

## 4. نتائج التحقق الحالية

| الفحص | النتيجة المسجلة |
|---|---:|
| TypeScript `tsc --noEmit` | ناجح |
| Production Build | ناجح — 184 وحدة |
| Vitest | ناجح — 9 ملفات و64 اختبارًا |
| Laravel Feature Tests | ناجح — 137 اختبارًا و371 assertion |
| Seeder الحسابات والكتالوج والموردين | ناجح |
| Seeder 200 عملية شراء | ناجح |

هذه النتائج تثبت سلامة البناء والاختبارات الحالية، لكنها لا تثبت وحدها جاهزية تجربة المستخدم على كل أحجام الشاشات أو صحة الإعدادات خلف Cloudflare أو قدرة النظام على الاستمرار بعد انقطاع SSE أو قاعدة البيانات أو البريد.

## 5. مشكلات UX والموثوقية المكتشفة

### 5.1 الأولوية P0 — يجب إصلاحها قبل أي إطلاق عام

| المشكلة | الدليل الحالي | الأثر |
|---|---|---|
| رسائل الأخطاء ليست عربية بالكامل | `src/utils/apiError.ts` يستخدم رسائل إنجليزية مثل `Server error. Please try again later.` و`An unknown error occurred.` | الموظف العادي قد لا يفهم سبب الخطأ، كما قد تظهر تفاصيل تقنية غير مناسبة |
| معالجة رسائل API غير موحدة | يتم أخذ `responseData.message` أو `axiosError.message` مباشرة قبل الترجمة | قد تتسرب رسائل Laravel أو Axios التقنية إلى المستخدم |
| انتهاء الجلسة غير واضح للمستخدم | مسار 401 يحتاج رسالة عربية موحدة وإجراء واضح للعودة لتسجيل الدخول | فقدان الثقة والاعتقاد أن الطلب لم يُحفظ أو أن النظام تعطل |

### 5.2 الأولوية P1 — يجب إغلاقها قبل الإنتاج المؤسسي

| المشكلة | الأثر التشغيلي | المعالجة المقترحة |
|---|---|---|
| لا توجد صفحات عربية مخصصة لـ 404 و403 و500 | المستخدم يرى شاشة غير مفيدة أو يعود للمتصفح بلا توجيه | صفحات موحدة فيها عنوان واضح وزر العودة ولوحة التحكم ورقم مرجعي اختياري للخطأ |
| صفحة التفضيلات محلية ومحدودة | لا يستطيع المستخدم التحكم في أنواع الإشعارات الاختيارية | إضافة قسم إلزامي لا يمكن تعطيله وقسم اختياري يمكن التحكم فيه، مع حفظ محلي آمن في المرحلة الأولى |
| لا توجد دلالة واضحة لكل خطأ صلاحيات | قد يظن المستخدم أن البيانات اختفت | صفحة 403 عربية تشرح أن الوصول غير متاح دون كشف الصلاحيات الداخلية |
| الاعتماد على SQLite محليًا | غير مناسب للتزامن والنسخ الاحتياطي والتوسع الإنتاجي | الانتقال إلى MySQL/PostgreSQL قبل الإطلاق، مع اختبار migration وbackup/restore |
| الحسابات التجريبية وكلمة مرور موحدة | خطر أمني مباشر إذا خرجت إلى الإنترنت | عدم تشغيل Demo Seeder على الإنتاج، وفرض تغيير كلمة المرور عند أول دخول أو إنشاء حسابات حقيقية |
| اختبار SSE خلف Proxy | Cloudflare/Nginx قد يسبب buffering أو timeout | `no-cache` و`X-Accel-Buffering: no` وtimeout مناسب واختبار reconnect |
| غياب خطة مراقبة وإشعار أخطاء مركزية موثقة | تأخر اكتشاف الأعطال | سجلات Laravel، تدوير Logs، health check، مراقبة 5xx، ومؤشر فشل SSE |

### 5.3 الأولوية P2 — تحسينات تجربة الاستخدام والأداء

| المجال | الحالة الحالية | التحسين |
|---|---|---|
| Skeleton loaders | غير متوفر في معظم الصفحات حسب التدقيق | Skeleton خاص بالجداول والبطاقات والنماذج بدل شاشة فارغة أو spinner عام |
| Empty states | تحتاج توحيدًا في القوائم والتقارير | حالات فراغ عربية تشرح عدم وجود بيانات وتعرض الإجراء التالي المناسب |
| البحث والفلترة | متاحة في بعض الصفحات، لكنها ليست موحدة بالكامل | توحيد الرقم، مقدم الطلب، الحالة، القسم/المشروع، الأولوية، التاريخ، المورد، مع زر مسح وحفظ آخر فلترة محليًا |
| منع طلبات API المكررة | توجد قابلية للتكرار عند إعادة العرض أو تغير المراجع | تثبيت query inputs، debounce للبحث، وإلغاء الطلب السابق عند تغير الفلترة |
| الجداول على الهاتف | تحتاج اختبارًا عمليًا لكل صفحات القوائم | أعمدة أولوية، تمرير أفقي مضبوط، وتحويل الصف إلى ملخص قابل للفتح على الشاشات الضيقة |
| النوافذ المنبثقة | عولجت بقواعد `modal-top-viewport` | يجب إضافة اختبار بصري لكل Modal على viewport صغير وكبير |
| الطباعة | تحسنت بصورة كبيرة | اختبار PDF/طباعة فعلية على Chrome مع بيانات عربية طويلة و200 عملية |

### 5.4 الأولوية P3 — تحسينات مستقبلية لا تمنع التشغيل

تتضمن هذه المرحلة حفظ عروض الفلترة المخصصة، سجل نشاط مرئي للمستخدم، دعم البريد الإلكتروني أو Push عند الحاجة، تحسينات الوصول Accessibility مثل التنقل بلوحة المفاتيح و`aria-label`، اختبارات أداء تحميل القوائم الكبيرة، ثم الانتقال إلى طابور Queue فعلي إذا أضيفت عمليات خارجية أو إشعارات بريدية كثيفة.

## 6. مراجعة المصادقة والصلاحيات

المصادقة الحالية تستخدم Bearer Token مع Laravel Sanctum. مسارات API المهمة محمية، ويجب الحفاظ على التحقق الخلفي حتى عند إضافة أي عنصر واجهة جديد. توجد مسارات للمدير العام والحسابات للعرض، لكن وجود أفعال `approve` و`reject` في بعض Controllers أو Routes حتى مع صلاحية العرض يستحق مراجعة دفاعية إضافية؛ يجب أن تمنع طبقة الخدمة أو Controller أي تغيير غير مسموح، لا أن تعتمد على تسمية الدور أو إخفاء الزر فقط.

المطلوب قبل الإنتاج هو اختبار مصفوفة الصلاحيات لكل دور: موظف، مراجع، مدير مشتريات، حسابات، مدير عام، Admin. يجب أن يغطي الاختبار الوصول المسموح، والرفض 403، ومحاولة الوصول إلى سجل خارج النطاق، ومحاولة استدعاء Endpoint مباشر بدون زر من الواجهة.

## 7. مراجعة البيانات وقاعدة البيانات

البيانات التجريبية الحالية مناسبة للاختبار، لكنها ليست بيانات إنتاج. يجب الفصل بين بيانات Demo وبيانات الشركة الحقيقية، وعدم تشغيل `DemoUserSeeder` أو Seeder العمليات الـ200 على قاعدة الإنتاج. يجب تنفيذ النسخ الاحتياطي قبل كل Migration، والتحقق من الاستعادة على بيئة منفصلة، وتسجيل مدة النسخ وحجمه.

ينبغي استخدام MySQL أو PostgreSQL في الإنتاج لدعم التزامن والنسخ الاحتياطي والمراقبة بصورة أفضل من SQLite، مع مراجعة الفهارس على حقول البحث الشائعة: رقم الطلب، الحالة، المراجع، القسم، المورد، التاريخ، وأمر الشراء. لا ينبغي إضافة Tax/VAT/Discount أو تغيير الحسابات الحالية ضمن هذا التدقيق.

## 8. مراجعة الإشعارات اللحظية

مسار SSE الحالي هو `/api/v1/notifications/stream`. التصميم الصحيح هو أن تكون استجابة هذا المسار غير قابلة للكاش، وأن يرسل الخادم heartbeat دوريًا، وأن يعيد العميل الاتصال عند انقطاع الشبكة مع منع إنشاء عدة اتصالات متوازية. يجب ألا تُستخدم Cloudflare Cache Everything على API أو SSE.

تدعم منصات المشتريات المؤسسية الحديثة البحث المخصص والإشعارات القابلة للضبط، لكن مع الفصل بين الإشعار الذي يتطلب إجراءً والإشعار المعلوماتي. توضح Coupa وجود تفضيلات لقنوات الإشعار وإمكانية إنشاء عروض مخصصة بشروط وأعمدة وترتيب، بينما توثق Oracle وصول إشعارات الموافقات داخل التطبيق والبريد وتؤكد ضرورة تحسينها للهاتف [1] [2] [3]. يترجم ذلك في نظام الإشبيليّة إلى إعدادات اختيارية للمستخدم، مع إبقاء إشعارات المهام الأساسية إلزامية.

## 9. مراجعة الاستجابة والتصميم العربي

الواجهة RTL عربية، وتستخدم Tailwind، وتحتوي قواعد تثبيت للنوافذ المنبثقة أعلى منتصف الشاشة. يجب اعتبار الهاتف حالة أساسية وليست نسخة مصغرة من الكمبيوتر: الأزرار الحرجة يجب أن تبقى مرئية، والجداول يجب ألا تدفع الإجراء الرئيسي خارج الشاشة، والنماذج الطويلة يجب أن تعرض أخطاء الحقول بجوار الحقل نفسه.

يُوصى باستخدام أحجام لمس لا تقل عن 44 بكسل تقريبًا للعناصر التفاعلية، وحالات تركيز مرئية، وعدم الاعتماد على اللون وحده لتمييز الحالة. ويجب اختبار اللغة العربية مع أسماء الموردين والأصناف الطويلة والأرقام العربية والإنجليزية المختلطة، لأن هذه الحالات قد تسبب انكسارًا بصريًا في الجداول والطباعة.

## 10. مراجعة النشر خلف Cloudflare Free

ملفات `deploy/production-checklist.md` و`deploy/cloudflare-free-production-ar.md` تغطي البنية المقترحة بنطاقين منفصلين للواجهة وAPI، وHTTPS بوضع Full (strict)، وتجاوز كاش API وSSE، وCORS، وحماية الأصل، والرؤوس الأمنية، وخطة التراجع. هذه نقطة قوة مهمة، لكنها تظل إعدادات موثقة وليست إثباتًا بأن النشر الفعلي تم اختباره من خارج الشبكة.

| موضوع | شرط الإطلاق |
|---|---|
| TLS | Cloudflare Full (strict)، شهادة صالحة على الأصل، Minimum TLS 1.2 |
| DNS | `app` و`api` Proxied، ولا يوجد DNS عام لقاعدة البيانات |
| Cache | Bypass كامل لـ `/api/*` وSSE، وكاش للأصول hashed فقط |
| Origin | جدار ناري يسمح بعناوين Cloudflare أو Tunnel، وSSH مقيد |
| CORS | نطاق الواجهة الإنتاجي فقط |
| Laravel | `APP_DEBUG=false`، `config:cache`، `route:cache`، `view:cache` |
| SSE | منع buffering، عدم التخزين، heartbeat، reconnect |
| Backup | نسخة يومية واختبار استعادة قبل الإطلاق |
| Rollback | نسخة build وقاعدة بيانات قبل كل إصدار أو migration |

## 11. مقارنة UX مرجعية مختصرة

هذه المقارنة لا تعني نسخ دورة عمل SAP أو Coupa أو Oracle، وإنما تستخدمها كمرجع لتحديد مستوى سهولة الاستخدام المتوقع من منتج SaaS مؤسسي.

| النمط المرجعي | ما يجب الاستفادة منه في نظام الإشبيليّة | ما لن يتم تغييره |
|---|---|---|
| SAP Ariba | فلاتر مخصصة للموافقات حسب الرقم والعنوان وآخر تحديث والمورد والمستخدم الطالب [1] | لا تغيير في مسار الموافقة أو عزل المراجع |
| Coupa | عروض جداول قابلة للتخصيص، شروط متعددة، أعمدة وترتيب، وتفضيلات إشعارات منفصلة [2] [3] | لا إضافة وظائف مشتريات خارج النطاق الحالي في هذه المرحلة |
| Oracle Procurement | إشعارات داخل التطبيق والبريد للمهمات، وتصميم الإشعار بصورة مناسبة للهاتف [4] | الإشعارات الحالية تظل داخل النظام وSSE، ولا يضاف بريد خارجي إلا بقرار مستقل |

## 12. خطة المعالجة حسب الأولوية

| الأولوية | الإجراء | معيار القبول |
|---|---|---|
| P0 | تعريب كل رسائل أخطاء API وAxios والأخطاء العامة | لا تظهر رسائل إنجليزية أو تقنية للمستخدم النهائي في حالات 401/403/404/409/422/5xx أو الاتصال |
| P1 | صفحات 404 و403 و500 عربية وربطها بالتوجيه وError Boundary | كل مسار غير معروف أو خطأ صلاحية أو خطأ واجهة يعطي رسالة عربية وزر عودة واضح |
| P1 | رسالة انتهاء جلسة عربية مع إزالة الجلسة وإعادة توجيه مفهومة | 401 يعرض تنبيهًا مفهومًا ولا يوحي بأن البيانات حُذفت |
| P1 | تفضيلات الإشعارات مع فصل الإلزامي والاختياري | لا يمكن تعطيل إشعار مهمة مطلوب، ويمكن تعطيل التفضيلات الاختيارية وحفظها |
| P1 | اختبار المصادقة والصلاحيات خلف نطاق HTTPS | نجاح المسموح ورفض غير المسموح، وعدم كشف بيانات قسم آخر |
| P2 | Skeleton وEmpty states موحدة | لا توجد صفحات بيضاء أثناء التحميل أو جداول بلا تفسير عند عدم وجود البيانات |
| P2 | Debounce وتثبيت مدخلات الاستعلام | لا توجد طلبات متكررة بسبب كل ضغطة أو إعادة Render |
| P2 | تحسين القوائم على الهاتف | الإجراء الرئيسي ظاهر، والبيانات الأساسية قابلة للقراءة دون كسر التخطيط |
| P3 | عروض فلترة محفوظة وسجل نشاط مرئي وتحسينات وصول | تحسينات قابلة للقياس دون التأثير على دورة المشتريات |

## 13. خطة الاختبار قبل الإطلاق

يجب تشغيل TypeScript وProduction Build وVitest وLaravel Tests بعد كل مجموعة تغييرات. ثم يجب إعادة Seeder Demo محليًا فقط، وتجربة السيناريوهات التالية ببيانات منفصلة: إنشاء طلب للتطوير وتوجيهه للمهندس سعود، إنشاء طلب للتنفيذ وتوجيهه لم. كمال سعيد، محاولة مراجع رؤية قسم آخر، تعديل المراجع للطلب واعتماده، اعتماد مدير المشتريات، إنشاء أمر شراء، وصوله للحسابات والمدير العام، فتح الطباعة من Chrome، وفقدان اتصال SSE ثم عودته.

يجب اختبار أحجام الهاتف والتابلت واللابتوب والكمبيوتر، واللغة العربية مع نصوص طويلة، وحالات 401 و403 و404 و409 و422 و500، وانقطاع API، ومهلة الشبكة، وعدم وجود بيانات، ووجود 200 عملية شراء مع الفلترة. كما يجب تنفيذ اختبار خارجي بعد النشر يثبت HTTPS و`Cf-Ray` وتجاوز كاش API وSSE وعدم إمكانية الوصول المباشر إلى الأصل.

## 14. الخلاصة والقرار

الأساس الوظيفي للنظام قوي ومناسب للانتقال من prototype إلى منتج مؤسسي، خصوصًا بعد توحيد حقول رقم القطعة والمنطقة، وإصلاح الطباعة، وإضافة تغيير كلمة المرور، وتغطية الاختبارات. الفجوة الحالية ليست في دورة المشتريات، بل في طبقة المنتج المحيطة بها: رسائل الخطأ، حالات الفشل، تفضيلات الإشعارات، التحميل والفراغ، واختبارات النشر الفعلية.

سيتم تنفيذ المعالجة بالترتيب التالي: **P0 تعريب الأخطاء، ثم P1 حالات الخطأ وانتهاء الجلسة والتفضيلات، ثم P2 تحسينات UX والأداء، ثم تشغيل الاختبارات النهائية وتحديث تقرير التنفيذ**. لن يتم تعديل منطق الموافقات أو الحسابات أو الصلاحيات التجارية ضمن هذه الخطة.

## المراجع

[1]: https://help.sap.com/docs/ariba/f5681fbd739744be88f4f5e8f8a49ead/7d6bbaa8bf194f1f8ba441f4de1da6f5.html "SAP Ariba — Improved Search for Purchase Requisitions in Your Approvals"

[2]: https://compass.coupa.com/en-us/products/product-documentation/supplier-resources/for-suppliers/coupa-supplier-portal/get-started-with-the-csp/your-account/create-custom-views "Coupa — Create Custom Views"

[3]: https://compass.coupa.com/en-us/products/product-documentation/supplier-resources/for-suppliers/coupa-supplier-portal/get-started-with-the-csp/your-account/view-and-manage-notifications "Coupa — View and Manage Notifications"

[4]: https://docs.oracle.com/en/cloud/saas/procurement/26b/oapro/configure-procurement-workflow-notifications.html "Oracle Procurement — Configure Procurement Workflow Notifications"
