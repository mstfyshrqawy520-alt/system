# تصور تطوير صفحة Admin لمراقبة النظام أثناء الـDeploy وProduction

## الهدف

تتحول صفحة Admin إلى **مركز مراقبة وتشغيل للنظام**، وليس مجرد شاشة لإدارة المستخدمين. تعرض الصفحة حالة التطبيق وقاعدة البيانات والإشعارات والـSSE والأخطاء والـDeploy، مع الحفاظ على دورة المشتريات كما هي دون أي تغيير.

يجب أن تكون صفحة المراقبة **Read Only افتراضيًا**. أي إجراء مؤثر، مثل تفعيل وضع الصيانة أو إعادة تشغيل خدمة، يحتاج صلاحية منفصلة وتأكيدًا واضحًا وتسجيلًا في سجل الأحداث.

## أولًا: لوحة الحالة العامة

تظهر الصفحة عند فتحها ببطاقات واضحة لحالة المكونات الأساسية:

| المؤشر | ما الذي يظهره | الحالة المقترحة |
|---|---|---|
| حالة التطبيق | هل Laravel API يعمل ويستجيب | سليم / تحذير / متوقف |
| قاعدة البيانات | نجاح الاتصال وزمن الاستجابة | متصلة / بطيئة / غير متصلة |
| واجهة React | آخر إصدار Build ورقم الـCommit | متوافق / يحتاج تحديث |
| الإشعارات Realtime | حالة SSE وآخر حدث تم بثه | يعمل / متأخر / متوقف |
| المصادقة | نجاح اختبار تسجيل الدخول وتجديد الجلسة | سليمة / تحذير |
| آخر Deploy | وقت النشر، الإصدار، والنتيجة | ناجح / جارٍ / فاشل |
| آخر فحص صحي | التاريخ والوقت ومدة الفحص | حديث / قديم |

يجب استخدام ألوان موحدة: الأخضر للسليم، البرتقالي للتحذير، الأحمر للعطل، والرمادي عندما تكون المعلومة غير متاحة. لا يجوز عرض حالة ناجحة مصطنعة إذا لم يتم ربط النظام فعليًا بمصدر القياس.

## ثانيًا: مركز الـDeploy

يُضاف تبويب باسم **حالة النشر** يعرض:

| العنصر | التفاصيل |
|---|---|
| الإصدار الحالي | رقم الإصدار أو Commit Hash الذي يعمل فعليًا |
| وقت آخر Deploy ناجح | التاريخ والساعة والمنطقة الزمنية |
| مدة البناء | وقت Build للواجهة ووقت Deploy للـLaravel إن توفر |
| نتيجة آخر Deploy | ناجح، فشل، تم التراجع، أو جارٍ |
| آخر Migration مطبقة | اسم آخر Migration ورقمها |
| Migrations معلقة | عدد migrations التي لم تُطبق، مع تحذير قوي |
| فحص بعد النشر | API health، اتصال DB، تسجيل الدخول، SSE، وإنشاء قراءة تجريبية آمنة |
| سجل الإصدارات | الإصدارات السابقة مع سبب التحديث والنتيجة |
| Rollback Status | يظهر فقط حالة التراجع، ولا ينفذ تلقائيًا من صفحة Admin |

الأفضل أن تستقبل الصفحة بيانات الـDeploy من Webhook آمن من بيئة الاستضافة أو من Backend Integration. إذا لم يتم إنشاء التكامل بعد، يجب عرض **غير متصل** بدلًا من اختراع بيانات.

## ثالثًا: مراقبة قاعدة البيانات

يُضاف تبويب **صحة قاعدة البيانات** لمراقبة:

| المؤشر | الفائدة |
|---|---|
| اتصال PostgreSQL | اكتشاف انقطاع قاعدة البيانات فورًا |
| زمن Query تجريبي | اكتشاف البطء قبل أن يؤثر على المستخدمين |
| حجم قاعدة البيانات | متابعة النمو والتخطيط للنسخ الاحتياطي |
| عدد الاتصالات | اكتشاف استهلاك Connection Pool |
| آخر Backup ناجح | التأكد من إمكانية الاستعادة |
| عمر آخر Backup | تحذير إذا تجاوز المدة المسموح بها |
| حالة Migrations | منع تشغيل نسخة بكود غير متوافق مع Schema |
| سلامة القيود | التحقق من `NOT NULL` للعناصر المهمة |
| عدد السجلات الرئيسية | Users، PR، PO، الموردون، الأصناف، System Events |
| Failed Jobs | ظهور المهام الفاشلة وإمكانية مراجعتها |

لا تظهر بيانات الاتصال أو كلمات المرور أو مفاتيح البيئة داخل الصفحة. يعرض Admin الحالة والأرقام التشغيلية فقط.

## رابعًا: مراقبة أداء الـAPI

يُضاف قسم **الأداء** ويعرض بيانات زمنية لآخر ساعة، 6 ساعات، 24 ساعة، و7 أيام:

| المؤشر | ما يجب قياسه |
|---|---|
| متوسط زمن الاستجابة | متوسط API response time |
| P95 latency | زمن استجابة 95% من الطلبات، وهو أهم من المتوسط وحده |
| معدل أخطاء 4xx | أخطاء الصلاحيات أو الطلبات غير الصحيحة |
| معدل أخطاء 5xx | أخطاء الخادم التي تحتاج تدخلًا |
| أكثر المسارات استخدامًا | مثل PR، Procurement، Accounting، Notifications |
| أبطأ المسارات | تحديد نقاط البطء مثل Analytics أو Reports |
| عدد المستخدمين النشطين | المستخدمون الذين نفذوا طلبًا خلال فترة محددة |
| معدل الطلبات | Requests per minute |
| حالات Timeout | عدد الطلبات التي تجاوزت المهلة |

تظهر الرسومات على شكل خطوط زمنية، مع فلاتر الفترة الزمنية، وبدون إدخال أي أرقام تجريبية إذا لم تتوفر Telemetry حقيقية.

## خامسًا: مراقبة Realtime وSSE

بما أن النظام يعتمد على الإشعارات الفورية، يجب أن يكون هناك قسم مستقل باسم **Realtime**:

| المؤشر | الغرض |
|---|---|
| عدد اتصالات SSE الحالية | معرفة عدد المستخدمين المتصلين لحظيًا |
| آخر Event تم بثه | التأكد من أن البث مستمر |
| آخر Event لكل مستخدم | اكتشاف مستخدمين متصلين لكن لا تصلهم إشعارات |
| عدد إعادة الاتصال | اكتشاف انقطاع الشبكة أو Proxy timeout |
| زمن وصول الإشعار | الفرق بين إنشاء الإشعار ووصوله للواجهة |
| إشعارات فشلت | اكتشاف فشل البث أو معالجة الإشعار |
| حالة Heartbeat | التأكد من بقاء الاتصال مفتوحًا |

يجب منع Cloudflare من تخزين استجابات SSE أو أي API مصادق عليه، ويجب أن يظهر في صفحة Admin إذا كان آخر بث قديمًا عن الحد المقبول.

## سادسًا: مراقبة دورة المشتريات من منظور تشغيلي

هذا القسم لا يغير الـWorkflow؛ هو يوضح إن كانت هناك طلبات عالقة:

| المؤشر | مثال على التنبيه |
|---|---|
| طلبات في حالة DRAFT منذ فترة طويلة | طلبات لم يرسلها الموظف |
| طلبات بانتظار المراجع | مراجعة متأخرة |
| طلبات بانتظار مدير المشتريات | عنق زجاجة في الاعتماد |
| أوامر بانتظار الحسابات | أمر لم تتم مراجعته ماليًا |
| آخر PO صدر | التأكد من استمرار الدورة |
| طلبات بلا نشاط | لا يوجد System Event منذ مدة |
| طلبات تجاوزت SLA | عرض العمر بالأيام والساعات |
| إشعارات غير مقروءة قديمة | احتمال أن المستخدم لا يتابع التنبيهات |

يمكن إظهار توزيع الحالات حسب القسم والمراجع والمستخدم، لكن دون إعطاء Admin قدرة على اعتماد أو رفض الطلبات إلا إذا كانت لديه صلاحية عمل مستقلة لذلك.

## سابعًا: مركز الأخطاء والسجلات

يُضاف تبويب **الأخطاء** يعرض آخر أخطاء التطبيق بطريقة آمنة:

| الحقل | الوصف |
|---|---|
| وقت الخطأ | التاريخ والساعة |
| نوع الخطأ | API، Database، Authentication، SSE، Queue |
| المسار | Endpoint أو الصفحة المتأثرة |
| Trace ID | رقم لتتبع الخطأ في Log الخادم |
| المستخدم | يظهر معرف المستخدم فقط عند الحاجة |
| الحالة | جديد، قيد التحقيق، تم الحل |
| عدد التكرار | يوضح إن كان الخطأ متكررًا |
| آخر ظهور | آخر مرة حدث فيها |

لا يجب عرض Stack Trace الكامل أو بيانات حساسة لكل Admin. يمكن إظهار التفاصيل الفنية الكاملة لصلاحية منفصلة مثل `system.view_sensitive_logs`.

## ثامنًا: التنبيهات التلقائية

يجب أن تدعم الصفحة قواعد Alert واضحة، مع تسجيل وقت فتح التنبيه ووقت حله:

| مستوى التنبيه | أمثلة عملية |
|---|---|
| حرج | API متوقف، قاعدة البيانات غير متصلة، Deploy فاشل، SSE متوقف، Migration ناقصة |
| مرتفع | أخطاء 5xx متكررة، Failed Jobs، Backup قديم، P95 latency مرتفع |
| متوسط | زيادة 401/403، تباطؤ Query، طلبات تجاوزت SLA |
| معلوماتي | Deploy ناجح، Backup ناجح، تغيير إعدادات Admin |

قواعد البداية المقترحة:

| القاعدة | إجراء التنبيه |
|---|---|
| خطأ 5xx مستمر أكثر من 5 دقائق | تنبيه حرج داخل Admin وإشعار Admin |
| P95 أعلى من ثانية لمدة 10 دقائق | تنبيه أداء مرتفع |
| آخر SSE event أقدم من دقيقتين | تنبيه Realtime |
| Failed Jobs أكبر من صفر | تنبيه تشغيلـي |
| وجود Migration معلقة | منع اعتبار Deploy ناجحًا وإظهار تحذير قوي |
| آخر Backup أقدم من 24 ساعة | تنبيه Backup |
| ارتفاع 401/403 بصورة غير معتادة | تنبيه أمني |
| طلبات بلا نشاط تتجاوز SLA | تنبيه Workflow |

## تاسعًا: سجل Admin Audit

يجب تسجيل كل ما يفعله Admin داخل النظام، مثل:

| العملية | ما يتم تسجيله |
|---|---|
| إضافة أو تعطيل مستخدم | الفاعل، المستخدم، الوقت، والقيم السابقة والجديدة |
| تغيير الدور أو الصلاحيات | الصلاحية قبل وبعد |
| تغيير إعدادات النظام | اسم الإعداد والقيمة السابقة والجديدة |
| تشغيل فحص صحي | الفاعل والنتيجة |
| تفعيل وضع الصيانة | السبب والمدة |
| قراءة Logs حساسة | الفاعل والوقت وسبب القراءة |
| إعادة إرسال إشعار | الإشعار المستهدف والسبب |
| تغيير قواعد التنبيهات | القاعدة القديمة والجديدة |

هذا السجل يجب أن يعتمد على `System Events` الموجود بالفعل في المشروع، مع إضافة نوعيات Admin محددة بدل إنشاء نظام تسجيل منفصل بلا داعٍ.

## عاشرًا: الإجراءات الآمنة داخل صفحة Admin

يمكن إضافة أزرار تشغيل، لكن بشكل محدود ومحمٍ:

| الإجراء | السماح المقترح |
|---|---|
| إعادة فحص النظام | مسموح دون تأثير على البيانات |
| إعادة إرسال إشعار فاشل | مسموح مع تسجيل العملية |
| اختبار SSE للمستخدم الحالي | مسموح |
| تنظيف Cache التطبيق | صلاحية منفصلة وتأكيد |
| وضع الصيانة | صلاحية منفصلة، سبب، ومدة انتهاء تلقائية |
| إعادة تشغيل Queue | من خلال الاستضافة وليس زرًا خطيرًا مباشرًا |
| Rollback | لا يتم من صفحة Admin في المرحلة الأولى؛ ينفذ من منصة النشر بعد مراجعة |
| حذف بيانات أو تعديل قاعدة البيانات | ممنوع من واجهة Admin |

## تصميم الواجهة المقترح

تكون صفحة Admin مقسمة إلى تبويبات واضحة:

1. **نظرة عامة**: حالة التطبيق، DB، SSE، Deploy، والتنبيهات الحرجة.
2. **حالة الـDeploy**: الإصدار، Commit، Migrations، ونتيجة الفحص بعد النشر.
3. **الأداء**: latency، الأخطاء، وعدد الطلبات.
4. **قاعدة البيانات**: الاتصال، النسخ الاحتياطية، الاتصالات، والقيود.
5. **Realtime**: SSE، Heartbeat، وإعادة الاتصال.
6. **دورة المشتريات**: الطلبات العالقة وSLA.
7. **الأخطاء والسجلات**: Error groups وTrace IDs.
8. **سجل Admin**: كل تغييرات الإدارة.
9. **إعدادات التنبيهات**: القواعد، القنوات، والأشخاص المستلمون.

على الموبايل، تتحول البطاقات إلى قائمة رأسية، وتصبح الجداول قابلة للتمرير الأفقي، ويظهر شريط الحالة والتنبيهات الحرجة أعلى الصفحة دائمًا.

## API والجداول المقترحة

### Endpoints

```text
GET  /api/v1/admin/system/health
GET  /api/v1/admin/system/metrics
GET  /api/v1/admin/system/deployments
GET  /api/v1/admin/system/database
GET  /api/v1/admin/system/realtime
GET  /api/v1/admin/system/alerts
POST /api/v1/admin/system/health-check
GET  /api/v1/admin/system/errors
GET  /api/v1/admin/system/audit-log
```

### الجداول الجديدة المحتملة

| الجدول | الغرض |
|---|---|
| `system_health_checks` | حفظ نتائج الفحوصات وزمنها |
| `system_alerts` | حفظ التنبيهات وحالاتها |
| `deployment_events` | حفظ بيانات النشر والإصدار |
| `system_metrics` | حفظ مؤشرات مجمعة، لا كل Request خام |
| `admin_audit_events` | يمكن استخدام `system_events` الحالي بدلًا منه إذا كان يغطي المطلوب |

لا يُنصح بتخزين كل Request خام داخل قاعدة PostgreSQL لفترات طويلة؛ الأفضل تجميع المؤشرات والاحتفاظ بالسجلات التفصيلية في Log provider أو خدمة مراقبة مناسبة.

## ترتيب التنفيذ المقترح

| المرحلة | الإضافات | الأولوية |
|---|---|---|
| قبل أول Deploy | Health endpoint، DB check، migration check، آخر Deploy، System Events، شاشة الأخطاء الأساسية | P0 |
| أول أسبوع Production | P95 latency، 5xx، Failed Jobs، SSE health، تنبيهات Admin، فحص Backup | P1 |
| بعد استقرار النظام | ربط Render/Cloudflare، سجل Deploy كامل، إحصائيات الموارد، SLA للطلبات | P2 |
| مرحلة متقدمة | Distributed tracing، Feature flags، Maintenance mode متقدم، تقارير دورية، تكامل Slack/Email | P3 |

## الخلاصة

أهم ما يجب تنفيذه قبل الـDeploy هو: **Health Check مركزي، مؤشر اتصال PostgreSQL، حالة Migrations، فحص SSE، مراقبة أخطاء 5xx، Failed Jobs، سجل Deploy، وتنبيهات Admin**. هذه العناصر تعطيك إجابة مباشرة عن الأسئلة الأهم: هل النظام يعمل؟ هل قاعدة البيانات سليمة؟ هل آخر Deploy ناجح؟ هل الإشعارات تصل؟ وهل هناك طلبات أو عمليات عالقة؟

أما حالة خادم Render، وCloudflare DNS/SSL، واستهلاك CPU والذاكرة، ونتيجة الـBuild التفصيلية، فلا ينبغي اختلاقها داخل التطبيق. إما أن يتم ربطها رسميًا عبر Webhook/API آمن، أو تظهر كـ **غير متصل** مع رابط مباشر إلى لوحة مزود الخدمة.
