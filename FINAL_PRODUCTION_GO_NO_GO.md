# تقرير QA النهائي قبل الإنتاج — قرار Go / No-Go

**النظام:** نظام مشتريات شركة الإشبيليّة

**التاريخ:** 15 أغسطس 2026

**إعداد:** Manus AI

## القرار التنفيذي

القرار الحالي هو **Conditional Go للاختبار الداخلي والـ Pilot المحدود فقط، وNo-Go للإطلاق العام على الإنترنت حتى استكمال بوابة النشر الفعلية**. التطبيق اجتاز الاختبارات التقنية الأساسية، واختبار دورة المشتريات الكامل، واختبارات الصلاحيات السلبية، وفحوص HTTP وCORS وSSE المحلية. لكن لا يجوز اعتبار ذلك نشرًا عامًا آمنًا قبل استبدال قيم البيئة التجريبية، وتطبيق Cloudflare وNginx فعليًا، وتهيئة قاعدة بيانات إنتاج ونسخ احتياطي قابل للاستعادة، وإكمال اختبار Chrome للصفحات الداخلية ومقاسات الهاتف المطلوبة.

هذا القرار لا يعني وجود عيب مثبت في دورة المشتريات. على العكس، الاختبارات الحالية تؤكد عدم تغيير المسار الأساسي: الموظف ينشئ الطلب، المراجع المختص يراجعه، مدير المشتريات يعتمد ويصدر أمر الشراء، ثم تصل الإشعارات للحسابات والمدير العام مع بقاء دوريهما للعرض فقط.

## ملخص الأدلة المنفذة

| المجال | الدليل الفعلي | النتيجة |
|---|---|---|
| TypeScript وProduction Build | `npx tsc --noEmit` و`npm run build` | ناجح؛ تم تحويل 187 وحدة في آخر Build |
| واجهة React | Vitest | 9 ملفات اختبار ناجحة |
| Laravel | `php artisan test --compact` | 137 اختبارًا ناجحًا و371 assertion |
| دورة المشتريات | `FullEndToEndWorkflowTest` | ناجح: PR ثم المراجعة ثم اعتماد المشتريات ثم PO ثم إشعارات الحسابات والمدير العام |
| العزل والصلاحيات | `AuthorizationTest` و`ReviewerPurchaseRequestTest` و`NotificationTest` | ناجحة؛ الحسابات والمدير العام لا يملكان قرار الاعتماد، والمراجع لا يرى خارج نطاقه |
| HTTP Login و`/auth/me` | اختبار حي مع Demo بعد إعادة seed | Login 200 و`/auth/me` 200 |
| 401 | طلب `/auth/me` بلا توكن مع `Accept: application/json` | 401 ورسالة عربية واضحة |
| 403 | موظف يحاول فتح إدارة المستخدمين | 403 ورسالة عربية واضحة |
| CORS | Preflight من `http://localhost:3000` | 204 مع السماح بالأصل المحلي المتوقع |
| Health | `/api/v1/health` | 200، قاعدة البيانات متصلة، ورؤوس عدم التخزين والأمان ظاهرة |
| SSE | `/api/v1/notifications/stream` | 200، `text/event-stream`، و`X-Accel-Buffering: no`، وعدم تخزين |
| بيانات Demo | `DemoUserSeeder` و`SeedTwoHundredPurchaseOperationsSeeder` | تم إنشاء الحسابات و200 عملية شراء بعد الاختبارات |
| صفحات الخطأ العامة | Chrome: `/404` و`/403` و`/500` | ظهرت بالعربية دون stack trace ظاهر أو كسر أفقي في اللقطات |
| صفحة الدخول | Chrome: `/login` | RTL وعناصر الدخول ورسالة انتهاء الجلسة ظهرت بالعربية |

## التغييرات المنفذة خلال QA النهائي

تم إصلاح الرسائل العربية المباشرة التي اكتُشفت أثناء الاختبار بدل الاكتفاء بتعريب الواجهة فقط. شمل ذلك رسائل login/logout، و401/403 في Middleware الصلاحيات، واستثناء `auth:sanctum` للطلبات JSON، واستجابات الإشعارات، ورسائل العزل في مسارات طلبات الشراء والمراجعين. لم تتغير أكواد HTTP أو صلاحيات الأدوار أو انتقالات الحالات.

تم تحسين Health Check ليعيد `503` وحالة `degraded` عند عدم اتصال قاعدة البيانات بدل إعادة نجاح كاذب بحالة HTTP 200. في الحالة السليمة ما زال يعيد 200 كما تثبت الاختبارات.

تم تعريب شاشة تفاصيل المراجع لمنع ظهور رموز الأولوية وإجراءات سجل المراجعة الداخلية الخام، وإضافة حد أدنى لأهداف لمس أزرار المكوّن الموحد على الهاتف مع حلقة تركيز مرئية، من دون تغيير الإجراء الذي ينفذه الزر.

## مصفوفة القرار

| البند | الحالة | مستوى الخطر | شرط الإغلاق |
|---|---|---:|---|
| سلامة Build واختبارات الواجهة | GO | منخفض | لا يوجد؛ تم التحقق الحالي |
| سلامة Laravel والـ API | GO | منخفض | لا يوجد؛ 137 اختبارًا ناجحًا |
| دورة المشتريات الأساسية | GO | منخفض | لا يوجد ضمن الاختبارات الحالية |
| عزل المراجع حسب القسم | GO | منخفض | لا يوجد ضمن الاختبارات الحالية |
| الحسابات والمدير العام View Only | GO | منخفض | لا يوجد ضمن الاختبارات الحالية |
| رسائل 401/403 ورفض الصلاحيات | GO | منخفض | الإبقاء على `Accept: application/json` في عملاء API |
| CORS المحلي والـ Bearer login | GO محليًا | متوسط | ضبط `CORS_ALLOWED_ORIGINS` على نطاق الواجهة الحقيقي فقط |
| SSE المحلي | GO محليًا | متوسط | تطبيق Nginx/Cloudflare bypass وtimeouts قبل الإنتاج |
| SSL وDNS وCloudflare | NO-GO حاليًا | عالٍ | إعداد النطاق والشهادات وFull (strict) وDNS وFirewall فعليًا |
| قاعدة بيانات الإنتاج | NO-GO حاليًا | عالٍ | MySQL/PostgreSQL إنتاجية، بيانات اعتماد حقيقية، وممنوع SQLite |
| النسخ الاحتياطي والاستعادة | NO-GO حاليًا | عالٍ | نسخة يومية مجدولة، اختبار Restore موثق، ونسخة قبل كل migration |
| Mobile Visual QA الكامل | OPEN | متوسط | فحص 320/360/375/390/414/768/1024 بكسل للصفحات الداخلية والنوافذ والجداول والطباعة |
| Chrome login التفاعلي | OPEN | متوسط | إعادة الاختبار من جلسة Chrome نظيفة أو فحص Network/Console؛ HTTP المباشر ناجح |
| Pagination لأرشيف PO | OPEN | متوسط | إضافة pagination أو حدود page size قبل نمو البيانات بصورة كبيرة؛ endpoint الحالي يستخدم `get()` |
| انتهاء التوكن تلقائيًا | OPEN | متوسط | اعتماد سياسة مدة توكن أو تدوير/إبطال رسمي قبل إطلاق عام واسع؛ Sanctum expiration الحالي غير محدد |
| الأسرار وDebug | GO مشروط | عالٍ إن خالف | `APP_DEBUG=false`، عدم رفع `.env`، وعدم تشغيل DemoSeeder على Production |

## تدقيق الأمان والإعدادات

قالب الإنتاج يضبط `APP_DEBUG=false`، ويستخدم MySQL بدل SQLite، ويقصر CORS على نطاق الواجهة، ويضبط Cookies آمنة، ويضيف HSTS في الإنتاج عند HTTPS. كما أن Middleware التطبيق يضيف `nosniff` و`SAMEORIGIN` و`Referrer-Policy` و`Permissions-Policy`، ويمنع التخزين المشترك لاستجابات API. تم التحقق حيًا من `Cache-Control` و`X-Content-Type-Options` على Health.

مع ذلك، قيمة `TRUSTED_PROXIES=*` لا تكون مقبولة إلا إذا كان الأصل لا يستقبل إلا اتصالات Cloudflare أو Proxy موثوق. يجب عدم ترك الأصل مكشوفًا مع هذه القيمة؛ لأن الثقة في الرؤوس المحولة تصبح غير آمنة عند السماح باتصال مباشر من الإنترنت. يجب تطبيق قوائم Cloudflare IP في جدار الحماية وNginx قبل اعتبار النشر مغلقًا.[1] [2]

تم فحص أسماء ملفات البيئة و`.gitignore` دون كشف قيم الأسرار. ملف `.env` غير ظاهر ضمن الملفات المتتبعة لأن المشروع المحلي لا يحتوي metadata Git في الجذر أثناء التدقيق، ولذلك يجب تكرار فحص عدم رفعه في مستودع الإطلاق الفعلي.

## تدقيق Cloudflare وNginx

ملفات النشر تتضمن قالبًا منفصلًا للواجهة وAPI، وتحويل HTTP إلى HTTPS، وبروتوكولات TLS 1.2 و1.3، وكاشًا طويلًا للأصول ذات hash، وإعادة تحقق لـ`index.html`. كما تتضمن تجاوز الكاش لكل `/api/` ولـSSE، وتعطيل buffering ورفع مهلات اتصال SSE. هذه الملفات **قوالب جاهزة وليست إعدادًا مطبقًا على حساب Cloudflare أو خادم فعلي**.

يجب تطبيق Cloudflare Cache Rules بالترتيب التالي: تجاوز API الخاص، تجاوز مسار SSE، كاش الأصول hashed فقط، وإعادة تحقق index.html. يجب عدم استخدام Cache Everything على API، وعدم وضع تحديات JavaScript على API أو SSE، واستخدام SSL/TLS بوضع Full (strict).[3] [4]

## تدقيق النسخ الاحتياطي والتراجع

التوثيق يطلب نسخة قبل كل migration ونسخة يومية، والاحتفاظ بـ`dist` السابق. لكن المستودع لا يحتوي سكربت Backup/Restore تشغيليًا مثبتًا؛ لذلك يبقى النسخ الاحتياطي **شرط إطلاق** يجب تنفيذه على خادم الإنتاج أو خدمة قاعدة البيانات، مع اختبار استعادة فعلي وليس مجرد وجود ملف SQL.

خطة التراجع المطلوبة هي: حفظ نسخة قاعدة البيانات قبل migration، حفظ build السابق، نشر build جديد، تنفيذ smoke tests، ثم إعادة `dist` السابق وتطبيق خطة migration عكسية آمنة عند الفشل. لا ينبغي تشغيل `DemoUserSeeder` أو Seeder العمليات على قاعدة Production.

## ملاحظات الأداء والتوسع

تم اختبار 200 عملية شراء تجريبية بنجاح. لكن endpoint أرشيف أوامر الشراء الحالي يعيد المجموعة كاملة عبر `get()` مع علاقات متعددة، والواجهة تقوم بالتصفية محليًا. هذا مقبول للـPilot وحجم 200 عملية، لكنه ليس استراتيجية توسع طويلة الأجل. قبل تخطي حجم البيانات الحالي يجب إضافة pagination و`page_size` وحدود استعلام وفلاتر خادمية، مع الحفاظ على نفس دورة القرار.

يستخدم قالب الإنتاج `QUEUE_CONNECTION=sync` وملفات cache/session. هذا مناسب لعقدة واحدة وPilot محدود، لكنه يحتاج Redis أو queue worker مضبوطًا عند الانتقال إلى حمل متزامن كبير أو أكثر من خادم. قالب Supervisor موجود كمرجع، لكنه لا ينبغي تفعيله مع `sync` إلا بعد تغيير إعداد queue وتطبيق اختبار مستقل.

## ملاحظات Chrome وMobile QA

نجح الفحص البصري عبر Chrome للصفحات العامة `/404` و`/403` و`/500` و`/login`، وظهرت العربية وRTL والأزرار ورسائل الخطأ دون stack trace أو تمرير أفقي ظاهر في اللقطات. تم التحقق من أن Chrome نفسه يصل إلى Health API.

لم يكتمل الفحص البصري للصفحات الداخلية لأن انتقال login في Chrome لم يحدث رغم نجاح login و`/auth/me` عبر HTTP المباشر بعد إعادة Demo. السبب المرجح في الجلسات السابقة كان أن اختبارات `RefreshDatabase` أزالت بيانات Demo قبل إعادة seed؛ بعد إعادة seed ظل الانتقال غير محسوم في قناة Chrome، ولذلك لا يتم إعلان نجاحه ولا اعتباره عيبًا مؤكدًا قبل DevTools Network/Console أو جلسة متصفح نظيفة.

ما زال يلزم فحص يدوي أو محاكاة للمقاسات 320 و360 و375 و390 و414 و768 و1024 بكسل، خصوصًا الجداول، النوافذ المنبثقة، اختيار التاريخ، القائمة الجانبية، والطباعة A4. تم فقط إضافة تحسين عام لأهداف لمس الأزرار إلى 44px على الشاشات الصغيرة.

## بوابة الإطلاق المطلوبة

لا ينتقل النظام إلى Public Production إلا بعد تنفيذ جميع البنود التالية:

1. تجهيز نطاقي `app` و`api` وشهادات TLS صالحة وتفعيل Cloudflare Full (strict) وAlways Use HTTPS.
2. ضبط `CORS_ALLOWED_ORIGINS` و`SANCTUM_STATEFUL_DOMAINS` على النطاقات الحقيقية فقط، وإعادة بناء الواجهة بعنوان API الإنتاج.
3. إغلاق منفذ الأصل أمام الاتصالات العامة أو قصره على عناوين Cloudflare، وعدم ترك قاعدة البيانات أو Redis مكشوفين.
4. اختبار login من النطاق العام، ثم PR كامل، عزل المراجع، اعتماد مدير المشتريات، إشعار الحسابات والمدير العام، والطباعة.
5. تنفيذ backup وrestore موثقين، وحفظ build سابق، ومراجعة خطة rollback.
6. إكمال Mobile Visual QA للمقاسات والصفحات الداخلية، وحسم login عبر Chrome.
7. إعادة تشغيل الاختبارات النهائية بعد إعداد البيئة الإنتاجية، ثم تشغيل smoke test خارجي من جهاز خارج الخادم.

## الخلاصة

**قرار Pilot داخلي:** Go.

**قرار Public Production الآن:** No-Go مشروط حتى إغلاق عناصر البنية التحتية والنسخ الاحتياطي وMobile/Chrome QA المذكورة أعلاه.

لا توجد نتيجة فشل في اختبارات دورة المشتريات أو الصلاحيات في هذه الجولة. المخاطر الحالية قبل الإطلاق العام تشغيلية وبنيوية أكثر من كونها منطق أعمال: Cloudflare غير مطبق فعليًا، النسخ الاحتياطي غير مؤتمت، أرشيف PO غير مرقّم خادميًا، واختبار المتصفح الداخلي لم يُغلق.

## المراجع

[1]: https://developers.cloudflare.com/fundamentals/security/protect-your-origin-server/ "Cloudflare: Protect your origin server"
[2]: https://www.cloudflare.com/ips/ "Cloudflare IP ranges"
[3]: https://developers.cloudflare.com/cache/how-to/cache-rules/settings/ "Cloudflare: Cache Rules settings"
[4]: https://developers.cloudflare.com/fundamentals/reference/http-headers/ "Cloudflare HTTP headers"
[5]: https://www.cloudflare.com/plans/free/ "Cloudflare Free Plan"

