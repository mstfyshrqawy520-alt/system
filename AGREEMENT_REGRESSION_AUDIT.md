# تدقيق رجوع المتطلبات المتفق عليها — نظام مشتريات الإشبيليّة

**التاريخ:** 15 أغسطس 2026

**الهدف:** تحديد العناصر التي فُقدت فعليًا من النظام مقارنة بالاتفاق الأصلي، وعدم تعديل أي جزء قبل فصل الفقد الحقيقي عن اختلاف مكان العرض أو اختلاف حالة البيانات.

## 1. قواعد الاتفاق التي تم اعتمادها للمقارنة

| القاعدة | الحالة المطلوبة |
|---|---|
| الدورة | الموظف ينشئ طلبًا، يختار المراجع، المراجع يراجع ويعدل ويعتمد أو يرفض، ثم مدير المشتريات صاحب القرار النهائي، ثم PO، ثم إشعار الحسابات والمدير العام |
| المراجع | يرى الطلبات المسندة إليه فقط حسب القسم/المراجع المختار، ولا يرى طلبات الأقسام الأخرى |
| الحسابات والمدير العام | View Only مع إشعار عند إصدار PO، دون اعتماد أو رفض أو تعديل دورة الطلب |
| إنشاء الطلب | صفحة واحدة، بدون مراحل منفصلة، وبدون مرفقات أو تاريخ استلام في نموذج الموظف |
| بيانات الطلب | اختيار المراجع، الأولوية، تاريخ الحاجة الافتراضي اليوم، الملاحظات، البنود |
| البند | اسم الصنف، رقم القطعة، المنطقة، الكمية، الوحدة العربية في العرض، والمواصفات |
| PO | المورد، اسم الصنف، رقم القطعة، المنطقة، الوحدة العربية، الكمية، سعر الوحدة، الإجمالي بالجنيه المصري EGP |
| الطباعة | طلب أو أمر منفصل، صفحة A4 واحدة، شعار، خط عربي عريض، رقم مسلسل أولًا، رقم القطعة والمنطقة، أسماء التوقيعات، الإجماليات رأسيًا |
| الحسابات والمدير العام | ظهور مقدم الطلب ورئيس القسم المعتمد والقسم والمورد والبنود والبيانات المالية للعرض |
| الضرائب والخصومات | غير موجودة في أي شاشة أو قالب أو API |
| الإشعارات | Realtime عبر SSE دون Refresh، مع بقاء إشعارات المهام الأساسية إلزامية |
| الإدارة | إدارة المستخدمين والأدوار والصلاحيات والأقسام والكتالوج والموردين والبيانات التجريبية |

## 2. العناصر المؤكد أنها لم تُفقد

| العنصر | الدليل الحالي |
|---|---|
| صفحة إنشاء الطلب الواحدة | `CreatePurchaseRequestPage.tsx` يعرض `PRWizardStep1` و`PRWizardStep2` داخل صفحة واحدة ويؤكد عدم وجود مراحل أو مرفقات |
| اختيار المراجع | `PRWizardStep1.tsx` و`getPurchaseRequestReviewerOptionsApi` موجودان |
| رقم القطعة والمنطقة في إنشاء الطلب | `PRWizardStep2.tsx` يفرضهما، و`CreatePurchaseRequestPage.tsx` يتحقق منهما قبل الإرسال |
| التاريخ الافتراضي | `CreatePurchaseRequestPage.tsx` يهيئ `date_needed` بتاريخ اليوم |
| المراجع والعزل | مسارات reviewer موجودة، وخدمة المراجعة و403 الخلفية موجودان |
| رقم القطعة والمنطقة في PO | `CreatePurchaseOrderPage.tsx` و`DirectPoModal.tsx` و`PurchaseOrderItemResource.php` موجودة |
| اسم الصنف والوحدة العربية في أغلب الشاشات | شاشات الحسابات ومدير المشتريات والطباعة تستخدم `item_name` أو `item_description` و`getUnitLabel()` |
| مقدم الطلب ورئيس القسم | `PurchaseOrderResource.php` يوفر `requested_by` و`department_approver` و`department`، والحسابات والطباعة تعرضها |
| Direct PO | التحقق الخلفي يفرض اسم الصنف ورقم القطعة والمنطقة، ثم `submitToAccounting()` يصدره فورًا |
| الإشعارات وSSE | مسارات `/notifications` و`/notifications/stream` وخدمة الإشعارات موجودة |
| الطباعة | `PurchaseOrderPrintModal.tsx` و`PurchaseRequestPrintModal.tsx` يحتويان الشعار والحقول المطلوبة |
| إدارة النظام | صفحات admin للمستخدمين والأدوار والصلاحيات والأقسام والتصنيفات والأصناف والموردين موجودة |
| البيانات التجريبية | DemoUserSeeder وSeedTwoHundredPurchaseOperationsSeeder و20 موردًا و300 عنصر كتالوج موجودة |

## 3. الفجوات المؤكدة التي تحتاج استعادة

| الأولوية | الفجوة | الدليل | المعالجة الآمنة |
|---|---|---|---|
| P1 | تفاصيل PO للمدير العام تعرض الوحدة الخام مثل `PCS` بدل الوحدة العربية | `GeneralManagerPurchaseOrderDetailsPage.tsx` يعرض `item.uom || 'PCS'` بدل `getUnitLabel(item.uom)` | إصلاح عرض فقط، دون تغيير API أو الحالة |
| P1 | تفاصيل PO للمدير العام تعرض `item_description` فقط، بينما الاتفاق يوحد العرض باسم «اسم الصنف» | نفس الصفحة لا تستخدم `item.item_name` رغم أن Resource يوفره | استخدام `item.item_name || item.item_description` وتسمية العمود «اسم الصنف» |
| P1 | أرشيف PO لا يحتوي تبويب/فلتر «تم الإصدار» | `PurchaseOrdersPage.tsx` يحتوي tabs للمسودة والحسابات والمرتجع والاعتماد والرفض لكنه لا يحتوي `ISSUED` | إضافة تبويب ISSUED وعدّه فقط، مع إبقاء الحالات القديمة للتوافق التاريخي |
| P1 | أرشيف PO ما زال يركز نصيًا على حالات مسودة/مراجعة مالية رغم أن الاتفاق التشغيلي يقول إن PO يصدر للحسابات عند الإنشاء | عنوان ووصف `PurchaseOrdersPage.tsx` ووجود `canEdit` للمسودات | تعديل العرض والوصف ليُبرز «الأوامر الصادرة»، دون حذف دعم الحالات التاريخية أو تغيير انتقالات API قبل اختبار مستقل |
| P1 | رابط «طلب الشراء المرتبط» في تفاصيل الحسابات والمدير العام يشير إلى `/purchase-requests/{id}` غير الموجود في `AppRoutes.tsx` | AppRoutes لا يحتوي مسارًا عامًا بهذا الشكل | استبداله بعرض نصي آمن أو ربطه بمسار الدور الصحيح دون كشف طلب خارج صلاحية الدور |
| P2 | شاشة الحسابات تعرض «اسم الصنف» و«وصف البند» معًا، ما قد يعطي انطباعًا بتكرار أو فقدان تسمية موحدة | `AccountingPurchaseOrderDetailsPage.tsx` | توحيد الاسم إلى «اسم الصنف» وإبقاء المواصفات كتفصيل منفصل، دون حذف بيانات API |
| P2 | Endpoint أرشيف PO يعيد كل الأوامر عبر `get()` دون pagination | `ProcurementPurchaseOrderController::indexPos()` | تحسين أداء لاحق مستقل؛ لا يُغيّر دورة الشراء في الإصلاح الحالي |

## 4. عناصر تحتاج تحققًا قبل اعتبارها مفقودة

| العنصر | سبب عدم الجزم |
|---|---|
| حالات PO_DRAFT | الخدمة تنشئ PO مسودة ثم تستدعي submit مباشرة، والـDirect PO يصدر فورًا؛ يلزم اختبار فشل submit لتحديد هل المشكلة مرئية للمستخدم أم مجرد انتقال داخلي تقني |
| تفاصيل الطلب في قائمة المدير العام | القائمة مختصرة عمدًا، والتفاصيل تعرض مقدم الطلب ورئيس القسم؛ لا تعتبر فقدًا قبل تجربة المستخدم |
| إعدادات الإشعارات عبر الأجهزة | الحفظ محليًا مقصود في هذه المرحلة وفق التقرير السابق، وليس فقدًا غير متوقعًا |
| تاريخ التوريد في PO | تاريخ الحاجة/التوريد في PO مختلف عن «تاريخ الاستلام» الذي أُزيل من نموذج الموظف؛ لا يُحذف قبل تأكيد أنه غير مطلوب في PO |
| عمود وصف البند | قد يكون مطلوبًا كمواصفات إضافية؛ المعالجة الآمنة توحيد التسمية لا حذف القيمة |

## 5. ما لن يتم تغييره أثناء الاستعادة

لن يتم تغيير انتقالات Purchase Request أو قرار مدير المشتريات أو الصلاحيات أو عزل المراجع أو القيم المالية أو العملة أو أي Tax/VAT/Discount. كما لن يتم حذف endpoint تاريخي أو حالة قديمة قبل التأكد من استخدامها في بيانات فعلية أو اختبارات.

## 6. ترتيب التنفيذ المقترح

يبدأ الإصلاح بالفجوات الأربع المؤكدة في عرض المدير العام وأرشيف PO وروابط الطلب، ثم تُعاد الاختبارات والبناء والـFeature Tests. بعد ذلك تتم تجربة البيانات التجريبية للتأكد من ظهور 200 عملية وأمر بحالة `ISSUED` في الأرشيف، ثم تُحدّث التقارير بنتائج الاستعادة.

## 7. نتائج المراجعة الإضافية

أثبتت المراجعة أن قائمة أوامر الشراء في الحسابات تعرض مقدم الطلب ورئيس القسم والمورد، لكنها لا تعرض **القسم** كعمود مستقل رغم أن الحسابات تحتاج معرفة القطعة التابعة لأي قسم. أضيفت هذه النقطة إلى الفجوات المؤكدة.

كما ثبت أن أرشيف مدير المشتريات كان يخلو من تبويب `ISSUED` رغم أن حالة الإصدار موجودة في `StatusBadge` والخدمة الخلفية. وثبت أن Direct PO يصدر للحسابات فورًا في الخادم، لكن الواجهة كانت توجه المستخدم بعد النجاح إلى شاشة تعديل الأمر، وهو سلوك يوحي بوجود مسودة؛ تم اعتبار ذلك رجوعًا واجهيًا مؤكدًا.

مكوّن `DeliveryTrackingWidget` وEndpoint تحديث حالة التوريد موجودان في المستودع، لكن المكوّن غير مستورد في أي صفحة مستخدم. لذلك لا يظهر تتبع التوريد فعليًا في الواجهة الحالية، ولا تم حذفه أثناء هذه الجولة حتى لا يتغير عقد API التاريخي دون طلب مستقل.

## 8. تصحيح بيانات Demo

اكتُشف أن `SeedTwoHundredPurchaseOperationsSeeder.php` كان يحتوي على `OPERATIONS_COUNT = 300` رغم أن اسمه والاتفاق يطلبان 200 عملية. تم إرجاع الثابت إلى 200. بعد إعادة تشغيل Seeder يجب أن يصبح عدد العمليات الموسومة التجريبية 200، مع احتمال وجود سجلات أساس غير موسومة من DemoSeeder.
