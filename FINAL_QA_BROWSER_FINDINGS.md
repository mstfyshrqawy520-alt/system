# نتائج التحقق البصري عبر Chrome

**التاريخ:** 15 أغسطس 2026

تم فتح الصفحتين التاليتين فعليًا عبر Chrome المحلي:

| الصفحة | النتيجة |
|---|---|
| `/404` | ظهرت صفحة عربية كاملة بعنوان «الصفحة غير موجودة»، مع وصف واضح وزري «العودة للوحة التحكم» و«العودة للصفحة السابقة». التخطيط RTL واضح، ولا يوجد محتوى خارج viewport أو تمرير أفقي ظاهر في اللقطة. |
| `/403` | ظهرت صفحة عربية كاملة بعنوان «لا تملك صلاحية الوصول»، مع شرح حماية بيانات الأقسام الأخرى وزري «العودة إلى صفحتي الرئيسية» و«العودة للصفحة السابقة». التخطيط RTL واضح، ولا يوجد محتوى خارج viewport أو تمرير أفقي ظاهر في اللقطة. |

المتصفح المستخدم هو **My Browser / Chrome**، ونجح التنقل إلى `http://localhost:3000/404` و`http://localhost:3000/403`. لم يتم اعتبار ذلك اختبارًا لكل مقاسات الهاتف؛ ما زال يلزم تنفيذ اختبار يدوي/محاكى عند المقاسات 320 و360 و375 و390 و414 و768 و1024 بكسل، إضافة إلى اختبار الجداول والنوافذ والطباعة وتسجيل الدخول.

لم يتم تغيير دورة المشتريات أو الصلاحيات نتيجة هذا الفحص البصري.

## الجولة الثانية من التحقق البصري

| الصفحة | النتيجة |
|---|---|
| `/500` | ظهرت صفحة عربية بعنوان «حدث خطأ مؤقت» مع شرح لا يكشف stack trace أو تفاصيل خادم، وزري «إعادة تحميل الصفحة» و«العودة للصفحة السابقة». لا يظهر انكسار أفقي في اللقطة. |
| `/login` | ظهرت صفحة دخول RTL كاملة بالعربية، مع البريد الإلكتروني وكلمة المرور وزر تسجيل الدخول. ظهرت رسالة انتهاء الجلسة العربية «انتهت جلسة الدخول. يرجى تسجيل الدخول مرة أخرى.» مع زر إغلاق واضح، ولم تظهر بيانات توكن أو تفاصيل تقنية. اللقطة أظهرت 4 بكسلات فقط أسفل viewport، وهو هامش بصري طفيف لا يمنع الاستخدام لكنه يستحق المراجعة في اختبار الهاتف الضيق. |

هذه الجولة تحقق بصري حقيقي في Chrome للصفحات العامة ومسار انتهاء الجلسة. لم تُختبر بعد القوائم التشغيلية والنوافذ والطباعة عند كل مقاسات الهاتف المطلوبة.

## الجولة الثالثة من التحقق البصري

تم إدخال البريد التجريبي `ahmed@gmail.com` وكلمة المرور التجريبية في صفحة الدخول بنجاح من حيث التفاعل مع الحقول. بقيت رسالة انتهاء الجلسة ظاهرة قبل الإرسال، وهي السلوك المتوقع من علامة الجلسة السابقة حتى يتم تنفيذ الدخول. لم يتم بعد الضغط على زر تسجيل الدخول أو تنفيذ أي إجراء شراء.

## الجولة الرابعة من التحقق البصري

بعد إدخال بيانات Demo والضغط على «تسجيل الدخول»، بقيت الصفحة على `/login` مع رسالة انتهاء الجلسة نفسها ولم يظهر انتقال إلى لوحة مدير المشتريات. لا يكفي هذا وحده لإثبات فشل المصادقة؛ اختبار HTTP المباشر قبل ذلك نجح في login و`/auth/me` بعد إعادة تشغيل Laravel وإعادة seed. يلزم إعادة اختبار مسار الدخول من Chrome بعد التأكد من أن Vite يعيد تحميل أحدث متغير `VITE_API_BASE_URL` وعدم وجود token/session قديم في التخزين المحلي. لم يتم تنفيذ أي إجراء شراء أو اعتماد.

## الجولة الخامسة من التحقق البصري

تمت إعادة إرسال نموذج الدخول عبر زر «تسجيل الدخول» ثم عبر مفتاح Enter، وبقيت الصفحة على `/login` دون ظهور رسالة فشل جديدة أو انتقال. بما أن login و`/auth/me` نجحا عبر HTTP المباشر بالحساب نفسه، فهذه نتيجة تحتاج عزلًا على مستوى قناة Chrome/Vite (متغير API أو اتصال المتصفح بالخادم المحلي) قبل اعتبارها عيبًا مؤكدًا في منطق المصادقة. لا يتم إعلان نجاح Visual QA للصفحات الداخلية حتى تُحسم هذه النقطة.

## الجولة السادسة من التحقق البصري

أكد Chrome الوصول المباشر إلى `http://localhost:8000/api/v1/health` واستلم JSON بحالة `ok` وقاعدة بيانات `connected`. بعد إعادة فتح `/login?qa=refresh` ظهر نموذج الدخول بصورة صحيحة، لكن بقي تنبيه انتهاء الجلسة ظاهرًا قبل إدخال البيانات. هذا يثبت أن API reachable من نفس المتصفح، ولا يحسم وحده سبب عدم الانتقال بعد submit؛ يلزم فحص Network/Console في DevTools أو إعادة التشغيل من جلسة Chrome نظيفة قبل اعتماد النتيجة كعيب في التطبيق.

## الجولة السابعة من التحقق البصري

أُعيد إدخال حساب Demo في صفحة دخول جديدة، وتم إرسال النموذج مباشرة من حقل كلمة المرور عبر `press_enter`. بقيت الصفحة على `/login?qa=refresh` مع رسالة انتهاء الجلسة، ولم يظهر انتقال أو رسالة API جديدة. وبما أن API قابل للوصول من Chrome وlogin المباشر عبر PowerShell ناجح، فهذه نقطة **QA مفتوحة** تحتاج فحصًا من DevTools Network/Console أو استخدام جلسة Chrome نظيفة/إعادة تشغيل Vite؛ لم يتم تغيير منطق المصادقة استجابةً لها قبل تحديد السبب.

## الجولة الثامنة من التحقق البصري

بعد إعادة Demo والعمليات التجريبية، تم الضغط على زر الدخول في Chrome مرة أخرى، وبقيت الصفحة على login دون انتقال. اختبار CORS/HTTP السابق أوضح أن نتيجة 401 كانت لأن الاختبارات الكاملة أزالت بيانات Demo قبل إعادة seed، وتمت إعادة seed بعدها؛ لذلك يلزم تشغيل login من جلسة جديدة أو التحقق من سجل Vite/Network قبل إعلان وجود عيب برمجي. لا يوجد دليل على تغيير دورة المشتريات أو تجاوز الصلاحيات.

