# إصلاح صلاحية إضافة المستخدم — نظام مشتريات الإشبيليّة

## المشكلة

عند فتح إدارة المستخدمين بحساب `admin@gmail.com` كان زر «إضافة مستخدم جديد» ظاهرًا، لكن عند الحفظ تظهر رسالة: «ليس لديك صلاحية لتنفيذ هذا الإجراء.»

## التشخيص

مسار إنشاء المستخدم هو `POST /api/v1/admin/users`، ومحمي بالصلاحية `system.users.manage`. تعريف Seeder يمنح دور `admin` جميع الصلاحيات، لكن الاعتماد على جدول ربط الأدوار والصلاحيات وحده قد يؤدي إلى رفض مؤقت إذا كانت قاعدة التطوير أو بيانات Demo غير متزامنة.

## الإصلاح

تم تعديل `app/Models/User.php` بحيث يُعامل دور `admin` كمدير نظام شامل عند فحص الصلاحيات. باقي الأدوار ما زالت تعتمد على صلاحياتها المحددة، ولم تتغير صلاحيات الموظف أو المراجع أو مدير المشتريات أو الحسابات أو المدير العام.

كما أُضيف اختبار Feature مباشر في `tests/Feature/Api/V1/AuthorizationTest.php` يختبر إنشاء المستخدم عبر endpoint الحقيقي.

## النتائج

| الفحص | النتيجة |
|---|---|
| AuthorizationTest | ناجح — 39 اختبارًا و54 assertion |
| اختبارات Laravel الكاملة | ناجحة — 138 اختبارًا و374 assertion |
| Login Admin حي | ناجح |
| `POST /admin/users` حيًا | HTTP 201 |
| إعادة تحميل قائمة المستخدمين | ناجحة |
| تعطيل مستخدم الاختبار بعد الإنشاء | HTTP 200 |

## الحساب المستخدم

البريد: `admin@gmail.com`

كلمة المرور: `123456`

## طريقة التحقق في Chrome

1. سجّل الخروج من الجلسة الحالية.
2. أعد تسجيل الدخول بحساب `admin@gmail.com`.
3. افتح «إدارة المستخدمين».
4. اضغط «إضافة مستخدم جديد».
5. أدخل الاسم والبريد وكلمة المرور، واختر القسم والدور عند الحاجة.
6. اضغط «حفظ البيانات».

إذا كانت شاشة قديمة مفتوحة من قبل الإصلاح، استخدم تحديثًا كاملًا للصفحة ثم سجّل الدخول من جديد حتى لا تبقى جلسة الواجهة القديمة في المتصفح.
