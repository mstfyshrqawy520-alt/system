# تقرير إصلاح التأخير في لوحة مدير المشتريات

## الملخص

تم تشخيص التأخير القوي في لوحة مدير المشتريات، وتبين أن السبب الأساسي هو تحميل تفاصيل كل طلب شراء وكل أمر شراء بشكل منفصل عند فتح الصفحة أو تغيير التبويب. تم تطبيق تحسينات على الواجهة والخادم بحيث تُحمّل القوائم الأساسية مرة واحدة، وتُحمّل التفاصيل الثقيلة عند الحاجة فقط.

## سبب المشكلة

كانت صفحة `ProcurementManagerPage.tsx` تنفذ طلبًا تفصيليًا لكل طلب شراء عند فتح تبويب الطلبات، كما كانت تنفذ طلبًا تفصيليًا لكل أمر شراء عند فتح الأرشيف أو التقارير. مع وجود 15 أمرًا و15 طلبًا، ينتج عن ذلك أكثر من 30 طلب HTTP إضافيًا فوق الطلبات الأساسية.

كذلك كان endpoint التحليلات يحمّل بنود كل أوامر الفترة إلى الخادم، رغم أن الرسومات وجدول التفاصيل يحتاجان بيانات البنود لأحدث الأوامر فقط.

## الإصلاحات المنفذة

| الجزء | ما تم تغييره |
|---|---|
| صفحة مدير المشتريات | حذف تحميل تفاصيل جميع الطلبات من `loadData` والاعتماد على بيانات القائمة الأساسية. |
| أرشيف أوامر الشراء | حذف تحميل تفاصيل كل الأوامر عند فتح الأرشيف أو التقارير. |
| الطباعة | تحميل تفاصيل أمر الشراء عند الضغط على «طباعة» فقط، ثم حفظها في ذاكرة الصفحة لإعادة استخدامها. |
| تحديث التبويبات | لم يعد تغيير التبويب يعيد تحميل كل البيانات أو يجمّد الواجهة. |
| حالات التحديث | إضافة `refreshing` لتحديث البيانات دون إزالة الجدول بالكامل أثناء العمليات اللاحقة. |
| قائمة أوامر الشراء في Laravel | إضافة eager loading جماعي للبنود، والمورد، والموظف، ورئيس القسم، وسجل الاعتماد بدل استدعاء endpoint التفاصيل لكل أمر. |
| قائمة طلبات الشراء | تحميل المراجع وسجل الاعتماد جماعيًا حتى تظهر بيانات رئيس القسم دون طلبات تفصيلية منفصلة. |
| تحليلات Laravel | تحميل البنود لآخر 10 أوامر معروضة فقط، مع إبقاء التجميعات والإجماليات معتمدة على استعلامات خفيفة. |

## الحفاظ على دورة العمل

لم يتم تغيير أي قاعدة عمل أو صلاحية أو انتقال حالة. ما زالت الدورة كما هي:

> الموظف ← رئيس القسم/المراجع ← مدير المشتريات ← أمر الشراء ← الحسابات والمدير العام للعرض والإشعار.

التعديل محصور في **طريقة وتوقيت جلب البيانات**، وليس في الاعتماد أو الرفض أو إنشاء أمر الشراء أو الإشعارات أو الحسابات المالية.

## الملفات المعدلة

- `src/pages/procurement/ProcurementManagerPage.tsx`
- `app/Http/Controllers/Api/V1/ProcurementPurchaseOrderController.php`
- `app/Services/ProcurementPurchaseRequestService.php`
- `app/Http/Controllers/Api/V1/ProcurementAnalyticsController.php`

## نتائج الاختبارات

| الاختبار | النتيجة |
|---|---:|
| فحص صياغة ملفات PHP المعدلة | ناجح بدون أخطاء |
| TypeScript | ناجح |
| Production Build | ناجح — 190 وحدة |
| Vitest | ناجح — 9 ملفات و65 اختبارًا |
| Laravel | ناجح — 138 اختبارًا و374 assertion |

## ملاحظة التحقق في Chrome

أثناء الفحص التفاعلي الأخير انتهت جلسة Chrome، لذلك ظهرت شاشة تسجيل الدخول بدل لوحة مدير المشتريات. هذه ليست مشكلة في البناء أو في كود الأداء. بعد تسجيل الدخول مجددًا بحساب مدير المشتريات يمكن اختبار الأرشيف والطباعة مباشرة.

## طريقة التحقق اليدوي

1. افتح `http://localhost:5173`.
2. سجّل الدخول بحساب مدير المشتريات.
3. افتح لوحة مدير المشتريات.
4. جرّب تبويب «الطلبات المعلقة»، ثم «أرشيف أوامر الشراء»، ثم «التقارير والتحليلات».
5. يجب أن تظهر القوائم دون انتظار تحميل تفاصيل كل صف.
6. اضغط «طباعة» على أي أمر؛ يتم جلب التفاصيل لذلك الأمر فقط ثم فتح نافذة الطباعة.
