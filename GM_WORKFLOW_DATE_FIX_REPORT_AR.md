# تقرير تعديل مسار المدير العام وتاريخ الاحتياج

## نطاق التعديل

تم دعم مسار طلبات المدير العام / المدير التنفيذي المهندس محمد عبدالكريم بحيث لا يحتاج الطلب إلى مراجع أو مدير قسم للموافقة، وينتقل بعد الإرسال مباشرة إلى مدير المشتريات.

في مسار عروض الأسعار، يقوم مدير المشتريات بإضافة الموردين والعروض، ثم تقوم الحسابات بالترشيح فقط، وبعدها ينتقل الطلب إلى المدير العام لاتخاذ القرار النهائي. تم منع ترشيح مدير القسم لطلبات المدير العام في طبقة الخدمة، مع إبقاء ترشيح الحسابات ومدير القسم مطلوبين للطلبات العادية.

## التغييرات المنفذة

- ضبط `PurchaseRequestService` لإفراغ `reviewer_user_id` لطلبات المدير العام عند الإنشاء والتعديل والإرسال.
- تثبيت حالة الإرسال لطلب المدير العام على `PENDING_PROCUREMENT_APPROVAL`.
- إعادة التحقق من `date_needed` عند الإرسال، مع رفض التاريخ القديم ورسالة عربية واضحة.
- ضبط نموذج إنشاء وتعديل الطلب على الحد الأدنى لليوم الحالي، مع رسالة تحقق قبل استدعاء API.
- إضافة وصف واضح في واجهة المدير العام يوضح الانتقال المباشر إلى المشتريات.
- تحديث `PurchaseQuoteService` ليقبل ترشيح `ACCOUNTING` فقط لطلبات المدير العام، ثم يفتح `PENDING_EXECUTIVE_QUOTE_DECISION`.
- تحديث إشعارات عروض الأسعار ورسائل الواجهة لتوضيح أن القرار النهائي للمدير العام.
- إضافة `requester_role` و`is_general_manager_requester` إلى Resource لدعم القرار في الواجهة دون الاعتماد على البريد الإلكتروني.
- تحديث Seeder السيناريوهات: المجموعات 13–16 تستخدم طلبات المدير العام، والمجموعات القابلة للإكمال تحصل على تواريخ اليوم أو المستقبل، بينما تبقى بيانات الأرشيف القديمة في المجموعة 27.
- إضافة إصلاح آمن للسيناريوهات الحالية المعلّمة فقط دون حذف سجلات أو تعديل بيانات غير الاختبار.

## نتائج التحقق

| الفحص | النتيجة |
|---|---|
| PHP lint للملفات المعدلة | ناجح دون أخطاء صياغة |
| اختبارات Laravel الكاملة | 168 اختبارًا ناجحًا / 563 assertion |
| اختبارات Vitest | 75 اختبارًا ناجحًا / 11 ملفًا |
| Vite production build | ناجح |
| السيناريوهات القابلة للإكمال بتاريخ قديم | 0 |
| عينة `TEST-PR-G13-01` إلى `TEST-PR-G16-01` | منشئ الطلب `mohamed@gmail.com`، و`reviewer_user_id = null` |

## عينات جاهزة للتجربة

- `TEST-PR-G13-01`: طلب مدير عام بانتظار القرار بعد ترشيح الحسابات.
- `TEST-PR-G14-01`: طلب مدير عام بانتظار القرار بعد قرار الحسابات.
- `TEST-PR-G15-01`: طلب مدير عام تم اختيار عرضه.
- `TEST-PR-G16-01`: طلب مدير عام مرفوض بعد قرار العروض.

## ملاحظة عن عدد السجلات

الفحص أظهر 560 طلبًا معلّمًا؛ العدد الأساسي هو 540 سيناريو، مع 20 سجلًا مساندًا إضافيًا لتجربة توزيع دفعة واحدة على أقدم مديونيتين في المجموعة 26. هذه السجلات لم تُحذف أو تُكرر أثناء الإصلاح.

## حسابات التجربة

المدير العام: `mohamed@gmail.com`، وكلمة المرور `123456`.

الحسابات: `hasan@gmail.com`، وكلمة المرور `123456`.

مدير المشتريات: `ahmed@gmail.com`، وكلمة المرور `123456`.
